function escapeHtml(value=''){return String(value).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#39;');}
function slugify(value=''){return String(value||'').toLowerCase().normalize('NFKD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9\u3040-\u30ff\u3400-\u9fff]+/g,'-').replace(/^-+|-+$/g,'')||'item';}
function buildSlugs(items){const used=new Map();return items.map((item,index)=>{const raw=item.slug||item.title||`memory-${index+1}`;const base=slugify(raw);const count=used.get(base)||0;used.set(base,count+1);return count?`${base}-${count+1}`:base;});}
function normalizeTags(tags){if(Array.isArray(tags))return tags.filter(Boolean).map(String);if(typeof tags==='string')return tags.split(',').map(s=>s.trim()).filter(Boolean);return[];}
function normalizeUrls(item={}){const raw=Array.isArray(item.urls)&&item.urls.length?item.urls:[item.url||''];return raw.map(v=>String(v||'').trim()).filter(Boolean).slice(0,3);}
function resolveImage(path=''){const p=String(path||'').trim();if(!p)return'';if(/^(https?:|data:|blob:)/i.test(p))return p;if(p.startsWith('/'))return p;return'/'+p.replace(/^\.\//,'');}
function ratioOf(item){const r=String(item.imageRatio||'').trim();return['1:1','4:3','3:4','2:3','16:9'].includes(r)?r:'4:3';}
function renderPage(site,item){const title=escapeHtml(item.title||'Untitled');const date=escapeHtml(item.date||'');const comment=escapeHtml(item.comment||'');const image=resolveImage(item.image||'');const fit=item.imageFit==='cover'?'cover':'contain';const ratio=ratioOf(item);const tags=normalizeTags(item.tags);const urls=normalizeUrls(item);const siteName=escapeHtml(site?.name||'MASATO NASU');const tagline=escapeHtml(site?.tagline||'Product Design · Computational Experiments · Personal Lab');const tagsHtml=tags.length?`<div class="tags">${tags.map(tag=>`<span class="tag">${escapeHtml(tag)}</span>`).join('')}</div>`:'';const linksHtml=urls.length?`<div class="actions">${urls.map((url,i)=>`<a class="btn${i===0?' primary':''}" href="${escapeHtml(url)}" target="_blank" rel="noopener noreferrer">${i===0?'OPEN LINK':`LINK ${i+1}`}</a>`).join('')}</div>`:'';const media=image?`<figure class="media" data-ratio="${escapeHtml(ratio)}"><img src="${escapeHtml(image)}" alt="${title}" style="object-fit:${fit}"></figure>`:'';return`<!doctype html><html lang="ja"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1.0"><title>${title} / ${siteName}</title><style>:root{--bg:#f5f5f2;--card:#fff;--text:#111;--muted:#666;--line:#d8d8d2}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Helvetica,Arial,"Hiragino Kaku Gothic ProN","Yu Gothic",sans-serif}a{color:inherit;text-decoration:none}.wrap{max-width:1180px;margin:0 auto;padding:28px 20px 72px}.site-title{font-size:64px;line-height:.95;font-weight:800;letter-spacing:-.03em;margin:0 0 18px}.nav{display:flex;gap:22px;flex-wrap:wrap;margin:0 0 30px}.nav a{font-size:18px}.nav a.active{font-weight:700}.eyebrow{display:inline-flex;align-items:center;min-height:34px;padding:0 12px;border-radius:999px;border:1px solid var(--line);background:#fafaf7;color:var(--muted);font-size:12px;letter-spacing:.06em;text-transform:uppercase;margin-bottom:26px}.back{display:inline-flex;align-items:center;min-height:38px;margin:0 0 28px;color:#444;font-size:13px;font-weight:700}.memory{background:var(--card);border:1px solid var(--line);border-radius:30px;padding:34px;box-shadow:0 2px 12px rgba(0,0,0,.035)}.head{display:flex;justify-content:space-between;gap:24px;align-items:flex-start;margin-bottom:26px}.kicker{font-size:11px;color:var(--muted);font-weight:750;letter-spacing:.12em;margin-bottom:9px}.memory h2{font-size:clamp(36px,6vw,68px);line-height:1;letter-spacing:-.04em;margin:0}.date{flex:0 0 auto;border:1px solid var(--line);background:#fafaf8;border-radius:999px;padding:9px 14px;color:var(--muted);font-size:14px}.media{margin:0 0 30px;background:#ecece8;border:1px solid var(--line);border-radius:22px;overflow:hidden;display:flex;align-items:center;justify-content:center;min-height:300px}.media[data-ratio="1:1"]{aspect-ratio:1/1}.media[data-ratio="4:3"]{aspect-ratio:4/3}.media[data-ratio="3:4"]{aspect-ratio:3/4}.media[data-ratio="2:3"]{aspect-ratio:2/3}.media[data-ratio="16:9"]{aspect-ratio:16/9}.media img{width:100%;height:100%;display:block}.comment{font-size:18px;line-height:1.95;white-space:pre-wrap;word-break:break-word;margin:0 0 26px;max-width:900px}.tags{display:flex;flex-wrap:wrap;gap:8px;margin-bottom:24px}.tag{font-size:12px;padding:8px 10px;border-radius:999px;border:1px solid var(--line);background:#fafaf8;color:#444}.actions{display:flex;gap:10px;flex-wrap:wrap}.btn{display:inline-flex;align-items:center;justify-content:center;min-height:42px;padding:0 16px;border-radius:999px;border:1px solid var(--line);background:#fff;font-size:13px;font-weight:700}.btn.primary{background:#111;color:#fff;border-color:#111}@media(max-width:640px){.wrap{padding:18px 12px 44px}.site-title{font-size:40px}.nav{gap:14px;margin-bottom:20px}.nav a{font-size:15px}.memory{padding:20px;border-radius:22px}.head{flex-direction:column;gap:14px}.date{align-self:flex-start}.comment{font-size:16px;line-height:1.85}.media{min-height:220px;border-radius:16px}}</style></head><body><div class="wrap"><h1 class="site-title">${siteName}</h1><nav class="nav"><a href="/">Home</a><a href="/portfolio.html">Portfolio</a><a href="/memory.html" class="active">Peripheral Memory</a><a href="/tools.html">Tools</a><a href="/about.html">About</a></nav><div class="eyebrow">${tagline}</div><br><a class="back" href="/memory.html">← BACK TO PERIPHERAL MEMORY</a><article class="memory"><div class="head"><div><div class="kicker">PERIPHERAL MEMORY</div><h2>${title}</h2></div>${date?`<div class="date">${date}</div>`:''}</div>${media}${comment?`<div class="comment">${comment}</div>`:''}${tagsHtml}${linksHtml}</article></div></body></html>`;}
export async function onRequestGet(context){let data=null;try{const raw=await context.env.SITE_DATA.get('site-data');if(raw)data=JSON.parse(raw);}catch(error){console.error('Memory KV read failed',error);}if(!data){try{const url=new URL('/api/data',context.request.url);const res=await fetch(url.toString());if(res.ok)data=await res.json();}catch(error){console.error('Memory API fallback failed',error);}}const items=Array.isArray(data?.memory)?data.memory:[];const slugs=buildSlugs(items);let requested=String(context.params.slug||'');try{requested=decodeURIComponent(requested);}catch(_){}const index=slugs.indexOf(requested);if(index<0)return new Response('Memory item not found',{status:404,headers:{'content-type':'text/plain; charset=utf-8','cache-control':'no-store'}});return new Response(renderPage(data?.site||{},items[index]),{headers:{'content-type':'text/html; charset=utf-8','cache-control':'no-store'}});}
